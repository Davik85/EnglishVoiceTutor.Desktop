<UserControl x:Class="EnglishVoiceTutor.Desktop.Views.LessonHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="900">
    <Grid Margin="24" Background="#FFF6F8FB">
        <Border MaxWidth="820"
                Margin="0,12"
                Padding="24"
                HorizontalAlignment="Center"
                VerticalAlignment="Stretch"
                Background="White"
                CornerRadius="16"
                BorderBrush="#FFE1E8F0"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <TextBlock Text="{Binding Title}" FontSize="30" FontWeight="SemiBold" Foreground="#FF1C2B3A" TextAlignment="Center" />
                        <TextBlock Margin="0,10,0,0" Text="{Binding Subtitle}" FontSize="14" Foreground="#FF3A4A5B" TextAlignment="Center" TextWrapping="Wrap" />

                        <TextBlock Margin="0,24,0,0" Text="{Binding EmptyHistoryText}" FontSize="14" Foreground="#FF3A4A5B" TextAlignment="Center" TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasHistory}" Value="False">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <ItemsControl Margin="0,18,0,0" ItemsSource="{Binding Items}">
                            <ItemsControl.Style>
                                <Style TargetType="ItemsControl">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasHistory}" Value="False">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ItemsControl.Style>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,12" Padding="16" Background="#FFF7FBFF" CornerRadius="12" BorderBrush="#FFE1E8F0" BorderThickness="1">
                                        <StackPanel>
                                            <TextBlock Text="{Binding CompletedAt, StringFormat={}{0:yyyy-MM-dd HH:mm}}" FontSize="12" Foreground="#FF587189" />
                                            <TextBlock Margin="0,6,0,0" TextWrapping="Wrap" FontSize="14" Foreground="#FF1E3A56">
                                                <Run Text="{Binding DataContext.TopicLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" FontWeight="SemiBold" />
                                                <Run Text=" " />
                                                <Run Text="{Binding TopicTitle}" />
                                                <Run Text=" • " />
                                                <Run Text="{Binding DataContext.SituationLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" FontWeight="SemiBold" />
                                                <Run Text=" " />
                                                <Run Text="{Binding SubtopicTitle}" />
                                                <Run Text=" • " />
                                                <Run Text="{Binding DataContext.LevelLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" FontWeight="SemiBold" />
                                                <Run Text=" " />
                                                <Run Text="{Binding SelectedLevel}" />
                                            </TextBlock>
                                            <TextBlock Margin="0,10,0,0" Text="{Binding DataContext.GoodLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" FontSize="14" FontWeight="SemiBold" Foreground="#FF1E3A56" />
                                            <TextBlock Margin="0,4,0,0" Text="{Binding GoodText}" FontSize="14" Foreground="#FF1C2B3A" TextWrapping="Wrap" />

                                            <TextBlock Margin="0,10,0,0" Text="{Binding DataContext.ImproveLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" FontSize="14" FontWeight="SemiBold" Foreground="#FF1E3A56" />
                                            <TextBlock Margin="0,4,0,0" Text="{Binding ImproveText}" FontSize="14" Foreground="#FF1C2B3A" TextWrapping="Wrap" />

                                            <ItemsControl Margin="0,10,0,0" ItemsSource="{Binding UsefulPhrases}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding StringFormat=• {0}}" FontSize="14" Foreground="#FF1C2B3A" TextWrapping="Wrap" Margin="0,2,0,0" />
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>

                <Button Grid.Row="1"
                        Content="{Binding BackButtonText}"
                        Command="{Binding BackCommand}"
                        Margin="0,20,0,0"
                        HorizontalAlignment="Center"
                        MinWidth="160"
                        Height="38"
                        Padding="18,0"
                        Background="White"
                        Foreground="#FF1E3A56"
                        BorderBrush="#FFCAD8E6"
                        Cursor="Hand" />
            </Grid>
        </Border>
    </Grid>
</UserControl>

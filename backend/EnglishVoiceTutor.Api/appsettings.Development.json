{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Note": "Do not store secrets in source control. Use secure environment variables or secret stores later."
}
